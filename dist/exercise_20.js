"use strict";
// Think of something you could store in a array. For example, you could make a list of mountains, rivers, countries, cities, languages, or anything else you’d like. Write a program that creates a list containing these items.
let list = ["K2", "Urdu", "Everest", "Lhotse", "Makalu", "Paris", "Manaslu", "Nanga Parbat", "K2", "Islamabad", "Everest", "Lhotse", "Makalu", "Cho Oyu", "Japan", "Nanga Parbat"];
// just printing in different ways. 
console.log(list); // printing complete array
console.log(list.length); //printing array's length
console.log(list[0]); //element at 0th index
console.log(list[list.length - 1]); //coz indexing starts from 0 and length starts from 1.
console.log(list[Math.floor(Math.random() * list.length)]);
console.log(list[Math.floor(Math.random() * list.length)]);
console.log(list[Math.floor(Math.random() * list.length)]);
console.log(list[Math.floor(Math.random() * list.length)]);
console.log(list[Math.floor(Math.random() * list.length)]);
console.log(list[Math.floor(Math.random() * list.length)]);
